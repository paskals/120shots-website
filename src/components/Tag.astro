---
const { tag, href, classNames } = Astro.props;
---

<span class=`font-overpass-mono ${classNames}`>
  <a
    class="bg-slate-600 text-[rgb(245,245,245)] bg-opacity-50 px-2 py-1 rounded lowercase no-underline inline-block"
    href={href ? href : ""}
  >
    {tag}
  </a>
</span>
